// Generated by PeakRDL-regblock - A free and open-source SystemVerilog generator
//  https://github.com/SystemRDL/PeakRDL-regblock

module openenoc_switch (
        input wire clk,
        input wire rst,

        input wire s_cpuif_req,
        input wire s_cpuif_req_is_wr,
        input wire [4:0] s_cpuif_addr,
        input wire [31:0] s_cpuif_wr_data,
        input wire [31:0] s_cpuif_wr_biten,
        output wire s_cpuif_req_stall_wr,
        output wire s_cpuif_req_stall_rd,
        output wire s_cpuif_rd_ack,
        output wire s_cpuif_rd_err,
        output wire [31:0] s_cpuif_rd_data,
        output wire s_cpuif_wr_ack,
        output wire s_cpuif_wr_err,

        input openenoc_switch_pkg::openenoc_switch__in_t hwif_in,
        output openenoc_switch_pkg::openenoc_switch__out_t hwif_out
    );

    //--------------------------------------------------------------------------
    // CPU Bus interface logic
    //--------------------------------------------------------------------------
    logic cpuif_req;
    logic cpuif_req_is_wr;
    logic [4:0] cpuif_addr;
    logic [31:0] cpuif_wr_data;
    logic [31:0] cpuif_wr_biten;
    logic cpuif_req_stall_wr;
    logic cpuif_req_stall_rd;

    logic cpuif_rd_ack;
    logic cpuif_rd_err;
    logic [31:0] cpuif_rd_data;

    logic cpuif_wr_ack;
    logic cpuif_wr_err;

    assign cpuif_req = s_cpuif_req;
    assign cpuif_req_is_wr = s_cpuif_req_is_wr;
    assign cpuif_addr = s_cpuif_addr;
    assign cpuif_wr_data = s_cpuif_wr_data;
    assign cpuif_wr_biten = s_cpuif_wr_biten;
    assign s_cpuif_req_stall_wr = cpuif_req_stall_wr;
    assign s_cpuif_req_stall_rd = cpuif_req_stall_rd;
    assign s_cpuif_rd_ack = cpuif_rd_ack;
    assign s_cpuif_rd_err = cpuif_rd_err;
    assign s_cpuif_rd_data = cpuif_rd_data;
    assign s_cpuif_wr_ack = cpuif_wr_ack;
    assign s_cpuif_wr_err = cpuif_wr_err;

    logic cpuif_req_masked;
    logic external_pending;

    // Read & write latencies are balanced. Stalls not required
    // except if external
    assign cpuif_req_stall_rd = external_pending;
    assign cpuif_req_stall_wr = external_pending;
    assign cpuif_req_masked = cpuif_req
                            & !(!cpuif_req_is_wr & cpuif_req_stall_rd)
                            & !(cpuif_req_is_wr & cpuif_req_stall_wr);

    //--------------------------------------------------------------------------
    // Address Decode
    //--------------------------------------------------------------------------
    typedef struct {
        logic info;
        logic forwarding_control;
        logic default_forwarding;
        logic forwarding_table;
    } decoded_reg_strb_t;
    decoded_reg_strb_t decoded_reg_strb;
    logic decoded_err;
    logic decoded_req_is_external;

    logic [4:0] decoded_addr;
    logic decoded_req;
    logic decoded_req_is_wr;
    logic [31:0] decoded_wr_data;
    logic [31:0] decoded_wr_biten;

    always_comb begin
        automatic logic is_valid_addr;
        automatic logic is_valid_rw;
        automatic logic is_external;
        is_external = '0;
        is_valid_addr = '1; // No valid address check
        is_valid_rw = '1; // No valid RW check
        decoded_reg_strb.info = cpuif_req_masked & (cpuif_addr == 5'h0) & !cpuif_req_is_wr;
        decoded_reg_strb.forwarding_control = cpuif_req_masked & (cpuif_addr == 5'h4);
        decoded_reg_strb.default_forwarding = cpuif_req_masked & (cpuif_addr == 5'h8);
        decoded_reg_strb.forwarding_table = cpuif_req_masked & (cpuif_addr >= 5'h10) & (cpuif_addr <= 5'h10 + 5'hf);
        is_external |= cpuif_req_masked & (cpuif_addr >= 5'h10) & (cpuif_addr <= 5'h10 + 5'hf);
        is_valid_rw |= cpuif_req_masked & (cpuif_addr >= 5'h10) & (cpuif_addr <= 5'h10 + 5'hf);
        decoded_err = '0;
        decoded_req_is_external = is_external;
    end
    logic external_wr_ack;
    logic external_rd_ack;
    always_ff @(posedge clk) begin
        if(rst) begin
            external_pending <= '0;
        end else begin
            if(decoded_req_is_external & ~external_wr_ack & ~external_rd_ack) external_pending <= '1;
            else if(external_wr_ack | external_rd_ack) external_pending <= '0;
            `ifndef SYNTHESIS
                assert_bad_ext_wr_ack: assert(!external_wr_ack || (external_pending | decoded_req_is_external))
                    else $error("An external wr_ack strobe was asserted when no external request was active");
                assert_bad_ext_rd_ack: assert(!external_rd_ack || (external_pending | decoded_req_is_external))
                    else $error("An external rd_ack strobe was asserted when no external request was active");
            `endif
        end
    end

    // Pass down signals to next stage
    assign decoded_addr = cpuif_addr;
    assign decoded_req = cpuif_req_masked;
    assign decoded_req_is_wr = cpuif_req_is_wr;
    assign decoded_wr_data = cpuif_wr_data;
    assign decoded_wr_biten = cpuif_wr_biten;

    //--------------------------------------------------------------------------
    // Field logic
    //--------------------------------------------------------------------------
    typedef struct {
        struct {
            struct {
                logic next;
                logic load_next;
            } operation_mode;
            struct {
                logic next;
                logic load_next;
            } pause_request;
        } forwarding_control;
        struct {
            struct {
                logic next;
                logic load_next;
            } bitmap;
        } default_forwarding;
    } field_combo_t;
    field_combo_t field_combo;

    typedef struct {
        struct {
            struct {
                logic value;
            } operation_mode;
            struct {
                logic value;
            } pause_request;
        } forwarding_control;
        struct {
            struct {
                logic value;
            } bitmap;
        } default_forwarding;
    } field_storage_t;
    field_storage_t field_storage;

    assign hwif_out.info.table_depth.value = 16'h1;
    assign hwif_out.info.num_of_interfaces.value = 6'h1;
    // Field: openenoc_switch.forwarding_control.operation_mode
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.forwarding_control.operation_mode.value;
        load_next_c = '0;
        if(decoded_reg_strb.forwarding_control && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.forwarding_control.operation_mode.value & ~decoded_wr_biten[0:0]) | (decoded_wr_data[0:0] & decoded_wr_biten[0:0]);
            load_next_c = '1;
        end
        field_combo.forwarding_control.operation_mode.next = next_c;
        field_combo.forwarding_control.operation_mode.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.forwarding_control.operation_mode.value <= 1'h0;
        end else begin
            if(field_combo.forwarding_control.operation_mode.load_next) begin
                field_storage.forwarding_control.operation_mode.value <= field_combo.forwarding_control.operation_mode.next;
            end
        end
    end
    assign hwif_out.forwarding_control.operation_mode.value = field_storage.forwarding_control.operation_mode.value;
    // Field: openenoc_switch.forwarding_control.pause_request
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.forwarding_control.pause_request.value;
        load_next_c = '0;
        if(decoded_reg_strb.forwarding_control && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.forwarding_control.pause_request.value & ~decoded_wr_biten[7:7]) | (decoded_wr_data[7:7] & decoded_wr_biten[7:7]);
            load_next_c = '1;
        end
        field_combo.forwarding_control.pause_request.next = next_c;
        field_combo.forwarding_control.pause_request.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.forwarding_control.pause_request.value <= 1'h0;
        end else begin
            if(field_combo.forwarding_control.pause_request.load_next) begin
                field_storage.forwarding_control.pause_request.value <= field_combo.forwarding_control.pause_request.next;
            end
        end
    end
    assign hwif_out.forwarding_control.pause_request.value = field_storage.forwarding_control.pause_request.value;
    // Field: openenoc_switch.default_forwarding.bitmap
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.default_forwarding.bitmap.value;
        load_next_c = '0;
        if(decoded_reg_strb.default_forwarding && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.default_forwarding.bitmap.value & ~decoded_wr_biten[0:0]) | (decoded_wr_data[0:0] & decoded_wr_biten[0:0]);
            load_next_c = '1;
        end
        field_combo.default_forwarding.bitmap.next = next_c;
        field_combo.default_forwarding.bitmap.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.default_forwarding.bitmap.value <= 1'h0;
        end else begin
            if(field_combo.default_forwarding.bitmap.load_next) begin
                field_storage.default_forwarding.bitmap.value <= field_combo.default_forwarding.bitmap.next;
            end
        end
    end
    assign hwif_out.default_forwarding.bitmap.value = field_storage.default_forwarding.bitmap.value;
    // External region: openenoc_switch.forwarding_table
    assign hwif_out.forwarding_table.req = decoded_reg_strb.forwarding_table;
    assign hwif_out.forwarding_table.addr = decoded_addr[3:0];
    assign hwif_out.forwarding_table.req_is_wr = decoded_req_is_wr;
    assign hwif_out.forwarding_table.wr_data = decoded_wr_data;
    assign hwif_out.forwarding_table.wr_biten = decoded_wr_biten;

    //--------------------------------------------------------------------------
    // Write response
    //--------------------------------------------------------------------------
    always_comb begin
        automatic logic wr_ack;
        wr_ack = '0;
        wr_ack |= hwif_in.forwarding_table.wr_ack;
        external_wr_ack = wr_ack;
    end
    assign cpuif_wr_ack = external_wr_ack | (decoded_req & decoded_req_is_wr & ~decoded_req_is_external);
    // Writes are always granted with no error response
    assign cpuif_wr_err = '0;

    //--------------------------------------------------------------------------
    // Readback
    //--------------------------------------------------------------------------
    logic readback_external_rd_ack_c;
    always_comb begin
        automatic logic rd_ack;
        rd_ack = '0;
        rd_ack |= hwif_in.forwarding_table.rd_ack;
        readback_external_rd_ack_c = rd_ack;
    end

    logic readback_external_rd_ack;

    assign readback_external_rd_ack = readback_external_rd_ack_c;

    logic [4:0] rd_mux_addr;
    logic [4:0] pending_rd_addr;
    // Hold read mux address to guarantee it is stable throughout any external accesses
    always_ff @(posedge clk) begin
        if(rst) begin
            pending_rd_addr <= '0;
        end else begin
            if(decoded_req) pending_rd_addr <= decoded_addr;
        end
    end
    assign rd_mux_addr = decoded_req ? decoded_addr : pending_rd_addr;

    logic readback_err;
    logic readback_done;
    logic [31:0] readback_data;
    always_comb begin
        automatic logic [31:0] readback_data_var;
        readback_data_var = '0;
        if(rd_mux_addr == 5'h0) begin
            readback_data_var[15:0] = 16'h1;
            readback_data_var[21:16] = 6'h1;
        end
        if(rd_mux_addr == 5'h4) begin
            readback_data_var[0] = field_storage.forwarding_control.operation_mode.value;
            readback_data_var[7] = field_storage.forwarding_control.pause_request.value;
            readback_data_var[15] = hwif_in.forwarding_control.pause_done.next;
        end
        if(rd_mux_addr == 5'h8) begin
            readback_data_var[0] = field_storage.default_forwarding.bitmap.value;
        end
        if((rd_mux_addr >= 5'h10) && (rd_mux_addr <= 5'h10 + 5'hf)) begin
            readback_data_var = hwif_in.forwarding_table.rd_data;
        end
        readback_data = readback_data_var;
        readback_done = decoded_req & ~decoded_req_is_wr & ~decoded_req_is_external;
        readback_err = '0;
    end

    assign external_rd_ack = readback_external_rd_ack;
    assign cpuif_rd_ack = readback_done | readback_external_rd_ack;
    assign cpuif_rd_data = readback_data;
    assign cpuif_rd_err = readback_err;
endmodule
